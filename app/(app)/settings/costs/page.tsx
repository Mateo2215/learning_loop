// Costs are now nested under Settings (not a top-level nav item). The original
// /costs route still exists for deep-links and bookmarks; this is the canonical
// home in the new IA. Re-mount the same server component.
export { default } from "../../costs/page";
