// Re-export for backwards compatibility — actual implementation moved to
// `section-header.tsx` to match the v2 design system naming.
export {
  SectionHeader as PageHeader,
  type SectionHeaderProps as PageHeaderProps,
} from "@/components/shared/section-header";
